spring:
  data:
    mongodb:
      host: localhost
      port: 37017
      authentication-database: admin
      username: special
      password: special
      database: special
  config:
    import:
      - auth.yml
      - s3_info.yml
